syntax = "proto3";
package chat;

option go_package = "github.com/arya-bhanu/intensive-go";

message Message {
    string body = 1;
}

message Email {
    string address = 1;
    string user = 2;
}

service ChatService {
    rpc SayHello(Message) returns (Message){}
}